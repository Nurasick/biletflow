services:
  db:
    image: postgres:17
    environment: [POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB]
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
  api: 
    build: ./backend
    env_file: .env
    depends_on: [db]
    ports: ["8000:8000"]
